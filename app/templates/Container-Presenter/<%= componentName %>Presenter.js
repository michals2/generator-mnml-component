import React from "react";

const <%= componentName %>Presenter = props => {
  const {
    // TODO: destructure props
  } = props;
  return (
    <div>
      {/* TODO: add jsx */}
    </div>
  );
};

export default <%= componentName %>Presenter;
